import axios from 'axios'

// const baseURL = axios.create({baseURL: 'http://localhost:8000'})
const baseURL = axios.create({baseURL: 'https://zezo-mern-chat-app-server.onrender.com'})

export default baseURL